function pairElement(str) {
  const basePairing = {
    A: "T",
    T: "A",
    C: "G",
    G: "C",
  };

  return str.split("").map((char) => [char, basePairing[char]]);
}

for (const iterator of object) {
}

w;
